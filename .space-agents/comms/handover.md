# Handover

**Date:** 2026-01-23 12:30
**Branch:** main
**Git:** Uncommitted changes (major refactor)

---

## What Happened This Session

Major restructuring of Space-Agents naming and organization:

1. **Commands renamed** - `run-*` → `houston-*` (houston-launch, houston-exploration, houston-mission, houston-capcom, houston-dock). Removed airlock, install, handover, mission-brief from user-facing commands.

2. **Skills reorganized**:
   - `mission-go*` → `mission*` (mission, mission-solo, mission-orchestrated, mission-ralph)
   - `pod` → `mission-pod`, `airlock` → `mission-airlock`
   - `exploration` → `exploration-brainstorm`, `mission-brief` → `exploration-plan`
   - Created `exploration` router skill (routes to brainstorm/plan/create/review/debug)

3. **Agents renamed**:
   - `exploration-*` → `brainstorm-*` (brainstorm-research, brainstorm-architecture, brainstorm-risk)
   - `planning-*` → `plan-*` (plan-task-planner, plan-sequencer, plan-implementer)
   - Mission agents updated: `worker` → `mission-worker`, `inspector` → `mission-inspector`, `analyst` → `mission-analyst`

4. **Dock skill rewritten** - Now focuses on narrative context, not Beads data duplication.

5. **Skill content trimmed** - Removed redundant sections in router skills (exploration, mission).

---

## Decisions Made

- **houston- prefix for commands** - Thematic (HOUSTON runs the show) and avoids conflict with skill names
- **Agents match skill prefixes** - brainstorm-*, plan-*, mission-* for consistency
- **Router pattern** - exploration and mission are routers that delegate to sub-skills
- **Task size guidance** - solo (1-3), orchestrated (4-10), ralph (10+) - removed "recommended" label

---

## Gotchas & Learnings

- The dock skill in plugin cache is stale - we updated it but need to publish/reinstall for changes to take effect
- Three exploration skills not yet implemented: exploration-create, exploration-review, exploration-debug

---

## In-Progress Work

Feature space-agents-1.2 (Agent Prompts and Terminology) was started but not completed - we pivoted to this refactoring work instead.

---

## Recommended Next Action

Commit all changes, then create the three missing exploration skills (create, review, debug) using existing superpowers skills as reference.

---

*Generated by /dock*
